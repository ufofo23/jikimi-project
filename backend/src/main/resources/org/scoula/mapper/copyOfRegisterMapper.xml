<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.codefapi.cor.mapper.CopyOfRegisterMapper">

    <insert id="insertCopyOfRegister" parameterType="org.scoula.codefapi.cor.dto.CopyOfRegisterDto">
        INSERT INTO COR_tbl (
            address,
            owner_state,
            ownership,
            common_owner,
            change_owner_count,
            maximum_of_bond,
            deposit
        ) VALUES (
            #{address},
            #{ownerState},
            #{ownership},
            #{commonOwner},
            #{changeOwnerCount},
            #{maximumOfBond},
            #{deposit}
        )
    </insert>

</mapper>
